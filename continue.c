#include "shell_header_files.h"
/**
 *continue_func - continues a particular set of codes
 *@buffer: buffer
 *Return: Nothing
 */
void continue_func(char *buffer)
{
	if (!buffer)
	{
		write_to_stderr("ssh: allocation error\n");
	}
}
